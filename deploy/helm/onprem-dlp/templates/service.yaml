apiVersion: v1
kind: Service
metadata:
  name: {{ include "onprem-dlp.fullname" . }}
  labels:
    {{- include "onprem-dlp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "onprem-dlp.selectorLabels" . | nindent 4 }}
